// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("mysqlasync", "for mysqlasync support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("mysqlasync", "enable mysqlasync support", "no");

if (PHP_MYSQLASYNC != "no") {
	EXTENSION("mysqlasync", "mysqlasync.c");
}

